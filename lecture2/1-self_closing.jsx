/* 
- in HTML we are using opening and closing tag
and also we have some elements that is self closing like <img> and <input>
which called self-closing tag

- when you write the self-closing tag its an optional to include forward-slash 
- but in jsx you have to include the slash 
 */

import React from 'react'
import ReactDOM from 'react-dom'


<img/>

